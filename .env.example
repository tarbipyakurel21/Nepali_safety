# Copy to .env and set your Hugging Face token (read access to gated models).
#   cp .env.example .env
# Get a token: https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# Optional overrides (defaults are fine on the cluster)
# HF_HOME=$HOME/caches/hf
# CONDA_ENV=$HOME/myenv
